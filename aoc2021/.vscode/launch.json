{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "AoC run binary",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            "preLaunchTask": "AoC build all go files",
            // binary has same name as current folder
            "program": "${relativeFileDirname}/${relativeFileDirname}",
            "cwd": "${relativeFileDirname}"
        }
    ]
}